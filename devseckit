#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"

if [[ "${1:-}" == "platform" ]]; then
  shift
  exec python3 -m devsec_platform.cli "$@"
fi

exec python3 "$SCRIPT_DIR/devseckit.py" "$@"
